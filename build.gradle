buildscript {
  ext {
    versions = [
        junit: "1.0.0-M4",
        jupiter: "5.0.0-M4"
    ]
  }

  repositories {
    jcenter()
  }

  dependencies {
    classpath "org.junit.platform:junit-platform-gradle-plugin:${versions.junit}"
  }
}

subprojects {
}